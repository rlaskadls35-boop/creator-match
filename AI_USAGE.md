# AI 활용 기록

## 2026-09-12 · 입력 폼 첫 화면

- 도구: Codex. 자연어 지시를 작은 화면 단위로 구현하고 로컬 브라우저에서 검증하기 위해 사용.
- 사용자 지시: 한 번에 전체 기획·구현하지 않고 Next.js, React, TypeScript로 흰색·파란색 입력 화면부터 개발. 예산 입력, 카테고리 드롭다운, 팔로워 규모 버튼 3개 요청.
- 제공 맥락: 사용자가 연결한 모비데이즈 P&A 과제 안내와 작업 폴더의 원본 자료. 과제에서 카테고리 다중 선택, 숫자 예산, 나노·마이크로·매크로 구분을 확인.
- AI의 초기 판단: 예산 입력 방식은 질문으로 확인 요청하고 과제 기준인 직접 입력을 우선 적용. 팔로워 구간은 임시로 나노 1만 미만, 마이크로 1만 이상 10만 미만, 매크로 10만 이상으로 정의. 추천 로직과 결과 화면은 이번 범위에서 제외.
- 구현: 금액 표시·추가, 다중 선택, 규모 선택, 실시간 요약, 필수값 검증, 초기화. 전체 과제에 대한 PRD와 추천 기준은 아직 확정하지 않음.
- 사용자 검토와 수정 판단: 사용자가 드롭다운 대신 카테고리가 항상 펼쳐진 다중 선택 버튼으로 수정 요청. 선택 항목을 한눈에 보고 누를 수 있도록 반영. 예산은 직접 입력으로 확정했으며 CSV 평균 예산을 기준으로 한 필터링 의미를 설명한 뒤 입력 화면 미리보기부터 실행 요청. 추천 결과는 아직 구현하지 않음.

## 화면 주석 반영

- 사용자가 상단 소개와 오른쪽 조건 요약 삭제, 입력 폼 너비 확대를 요청. 오른쪽 보조 문구를 포함해 단일 입력 폼으로 정리하고 화면 너비를 사용하도록 수정. 확인 메시지에서도 삭제된 요약 안내를 제거.
- 이후 사용자 요청으로 플랫폼 선택(전체/유튜브/인스타그램)을 추가. 전체를 기본값으로 두고 초기화에도 반영. 사용자가 명시적으로 요청하기 전에는 브라우저로 검토하지 않도록 지시하여 이번 변경은 타입 검사로 확인.

- 사용자가 흐린 글자, 과도한 여백, 복잡한 배치를 지적하여 UI UX Pro Max 기준으로 텍스트 시안 3개를 제시. 사용자가 선택한 1안(가로 행 정렬)을 반영해 반복 설명과 팔로워 장식을 제거하고 글자 대비 및 버튼 크기를 조정. 초기화는 제목 옆으로 이동하고 기존 입력·선택 동작을 유지. 브라우저 검토 없이 타입·린트·빌드로 검증.

## 조건별 크리에이터 결과표

- 사용자 지시: 조건 확인 시 CSV 조건에 맞는 데이터 테이블 표시. 컬럼 제안 검토 후 내부 ID는 숨기고, 플랫폼은 크리에이터 오른쪽에 별도 컬럼으로 배치하며, 캠페인 건수는 ‘누적 캠페인 건수’로 표시하도록 확정.
- 구현: 원본 CSV를 서버에서 읽어 타입을 변환하고 예산·카테고리·플랫폼·규모의 교집합을 결과표에 표시. 복수 카테고리는 합집합으로 처리. 결과가 없으면 안내 문구를 표시하고 입력 변경 및 초기화 시 결과를 비움.
- 판단: 예산은 과거 평균 진행 예산 이하의 후보 검색 기준으로 적용하고 현재 견적이 아님을 설명. 진행 이력이 없거나 예산이 0인 후보는 무료 후보로 취급하지 않고 제외 인원을 안내. 빈 평점은 ‘평가 없음’으로 표시. 추천 점수나 순위는 추가하지 않음.
- 검증: 예산 상한·팔로워 경계·복합 필터·CSV 파싱·원본 200행·컬럼 순서·빈 상태의 7개 테스트 통과. 린트, 타입 검사, 프로덕션 빌드 통과 및 원본 CSV 무변경 확인. 사용자 지시에 따라 브라우저 검토는 수행하지 않음.

## SQLite 저장 및 서버 조회로 전환

- 사용자 지시: 기존 CSV 직접 조회 구조를 설명받은 뒤 SQLite에 크리에이터 테이블 하나를 두는 구성으로 변경 요청. 과제의 안전한 데이터 처리 의미를 질문하여 NULL과 0의 차이, 예산 미확인 제외, 팔로워 경계를 먼저 설명하고 사용자의 ‘진행해’ 승인 후 전환.
- 설계: CSV 원본과 11개 컬럼을 유지하는 `creators` STRICT 테이블 하나. ID 기본 키, 숫자·범위·선택값 제약, 공란 NULL, 0원 원본 보존. 별도 DB 서버 없이 Node.js 내장 SQLite 사용. 최소 Node.js 22.13.0 조건과 실행 시 자동 적재를 README에 기록.
- 구현: 전 행 검증 후 트랜잭션과 ID 기반 갱신으로 적재하여 오류 시 롤백하고 중복 방지. ‘조건 확인하기’가 서버 API를 호출하면 SQLite에서 조건을 조회하도록 변경. 전체 후보를 첫 화면에 전달하던 방식 제거. 입력 재검증과 매개변수 바인딩 적용.
- 화면: 기존 컬럼과 필터 기준 유지. 조회 중 표시, 요청 실패 안내 추가. 조건 변경·초기화 시 요청 취소 및 오래된 응답 무시. 추천 점수나 추가 화면은 이번 범위에서 제외.
- 검증: SQLite 및 API 입력·결과표 관련 테스트 12개, 타입 검사, 린트, 프로덕션 빌드 통과. 원본 200행·NULL 평점 27개·0원 예산 27개를 확인했고 로컬 API에서 정상 조회, 결과 없음, 잘못된 예산 400 응답 및 초기 HTML에 전체 데이터가 포함되지 않음을 확인. 브라우저는 사용하지 않음.

- 사용자 요청으로 캠페인 조건과 결과표의 공통 최대 너비를 1,088px에서 1,440px로 확대. 데스크톱 결과표는 컬럼 너비 합계를 100%로 배분하고 긴 내용은 줄바꿈하여 가로 스크롤 없이 표시하도록 변경. 720px 이하 모바일에서는 가독성을 위해 표의 가로 스크롤을 유지. 브라우저 검토는 수행하지 않음.
