# Creator Match PRD

이 문서는 제출 시점의 실제 동작을 설명합니다. 초기 설계·변경 과정은 `docs/`에 보존하며, 최종 기능과 수치는 이 문서를 기준으로 합니다.

## 해결하려는 문제

광고주가 크리에이터를 찾을 때 팔로워 수만으로는 예산 적합성, 시청자 반응, 조회 규모, 협업 평가를 함께 판단하기 어렵습니다. 기록이 없는 후보를 임의의 수치로 채우면 검증된 성과처럼 보일 수 있고, 검색 결과가 없으면 다음 행동을 정하기 어렵습니다.

목표는 **조건에 맞는 후보를 추리고, 순위가 나온 이유를 확인하고, 조건이 맞지 않을 때 대안을 찾게 하는 것**입니다. 실제 섭외 가능성이나 광고 성과를 보장하는 서비스는 아닙니다.

## 대상 사용자와 핵심 흐름

광고주는 로그인 없이 플랫폼·1명당 예산·규모·복수 카테고리·캠페인 이력을 선택하고 검색합니다. 캠페인 이력이 있는 후보는 점수순으로, 없는 후보는 별도로 확인합니다. 지표별 재정렬과 계산 내역으로 비교한 뒤 검토할 후보를 고릅니다.

운영자는 임시 계정으로 같은 검색 화면에 접근하고 네 지표 비중을 조절합니다. 합계 100%일 때 미리보기하고 저장하며, 같은 브라우저에서 광고주 화면에 다시 진입하면 저장한 기준을 사용합니다.

## 기능 정의

| 기능 | 동작과 완료 기준 |
| --- | --- |
| 예산 입력 | 원 단위 양의 안전한 정수. 천 단위 쉼표와 읽기 쉬운 금액 표시. 미입력·0·처리 범위를 넘는 값은 검색 불가 |
| 카테고리 | 10개 중 하나 이상 다중 선택. 선택한 카테고리는 OR 조건 |
| 팔로워 규모 | 나노·마이크로·매크로 중 하나 필수 선택, 구간 표시 |
| 플랫폼·이력 | 전체/유튜브/인스타그램, 전체/이력 있는 후보만 선택. 검색 버튼을 누를 때 적용 |
| 이력 후보 | 플랫폼·카테고리·규모 일치, 평균 단가가 예산 이하, 점수에 필요한 실측값이 있는 후보를 표시 |
| 신규 후보 | 캠페인 0건이며 플랫폼·카테고리·규모가 맞는 후보. 예산 충족 미확인 문구와 별도 표 제공 |
| 설명 가능성 | 양쪽 역할 화면에서 실제 지표·산정식·등수·비중·기여 점수·합산값을 펼쳐 확인 |
| 재정렬 | 이력 후보는 매칭 점수·팔로워·조회수·참여율·건수·평점·단가·조회당 비용, 신규는 팔로워·조회수·참여율 |
| 후보 0명 | 조건 하나만 다른 근접 후보 최대 3명과 바꿀 조건. 근접 후보도 없으면 재검색 안내 |
| 후보 1~2명 | 기존 결과와 함께 예산·플랫폼·규모·카테고리 변경 선택지와 예상 인원 표시. 선택 시 폼 반영 후 재검색 |
| 데이터 오류 | 로딩 상태 표시. SQLite/WASM 다운로드·읽기 실패 시 오류 안내와 다시 시도 |
| 반응형 | 데스크톱은 비교 표, 좁은 화면은 후보별 정보 배치와 정렬 선택 제공 |

## 추천 기준

### 예산과 데이터의 의미

예산은 **크리에이터 1명과 진행할 캠페인 1건의 섭외 상한**입니다. 원본의 캠페인당 평균 진행 예산을 과거 단가로 사용하며 누적 집행 금액을 건수로 나눈 값으로 대체하지 않습니다. 신규의 0원 단가는 무료 섭외를 뜻하지 않으므로 파생 단가를 `null`로 둡니다. 공란 평점을 0점이나 평균 평점으로 채우지 않습니다.

### 규모 정규화

| 규모 | 팔로워 수 | 현재 인원 | 이력 / 신규 |
| --- | --- | ---: | ---: |
| 나노 | 15,000명 미만 | 101 | 90 / 11 |
| 마이크로 | 15,000명 이상 100,000명 미만 | 72 | 61 / 11 |
| 매크로 | 100,000명 이상 | 27 | 22 / 5 |

마이크로 안의 팔로워 격차를 줄이는 자체 기준으로 15,000명 경계를 선택했습니다. 인원을 균등하게 나누거나 추천 정확도를 보장하는 기준은 아닙니다. 경계가 바뀌면 비교 집단과 점수도 달라집니다.

### 항목과 가중치

| 항목 | 기본 비중 | 의도와 기준 |
| --- | ---: | --- |
| 조회 1회당 평균 비용 | 40% | 같은 규모에서 단가 대비 평균 도달이 큰 후보를 우선. 단가 ÷ 평균 조회수, 낮을수록 좋음 |
| 참여율 | 30% | 같은 규모에서 시청자·팔로워 반응이 높은 채널을 우선 |
| 평균 조회수 | 20% | 같은 규모에서 평균 도달이 큰 채널을 우선 |
| 광고주 평점 | 10% | 실제 평점이 있는 전체 후보 안에서 협업 평가를 보조 반영 |

비중은 데이터 탐색과 화면 비교를 거쳐 정한 기획 가설입니다. 실제 성과 레이블이 없는 더미 데이터에서 최적 가중치를 학습했다는 의미는 아닙니다. 운영자 화면에서 가설을 바꾸고 순위 변화를 확인할 수 있습니다.

단위가 다른 지표는 백분위 기반 0~100점으로 변환합니다. 비교 집단에서 더 좋은 값의 인원과 본인 제외 동점자 수의 절반을 합하고, 본인을 제외한 인원으로 나누어 상위 비율을 구한 뒤 100에서 뺍니다. 한 명뿐인 집단은 50점으로 처리합니다. 계산식과 재현 예시는 [README](README.md#추천-로직과-설계-근거)에 있습니다.

비교 집단은 검색 전 전체 데이터에서 고정합니다. 참여율·조회수는 같은 규모의 신규도 포함하고, 비용·평점은 결측값을 제외합니다. 검색 결과 한 명에게 자동으로 최고점을 부여하지 않습니다. 카테고리와 플랫폼까지 비교 집단을 세분하면 표본이 작아지므로 필터로만 사용합니다.

누적 캠페인 건수는 협업 경험 정보로 표시하고 정렬을 지원하지만 점수에는 넣지 않습니다. 집행 횟수가 많다는 사실을 높은 성과로 간주하지 않기 위한 선택입니다. 평점의 표본 수 보정은 적용하지 않습니다.

### 이력 없음과 대안 처리

신규는 실제 단가·평점이 없으므로 종합점수와 통합순위를 만들지 않습니다. 예산으로 제외하거나 예산에 맞는다고 판정하지 않고 별도 표에서 팔로워·조회수·참여율을 제공합니다. 사용자는 실제 견적과 협업 조건을 추가로 확인해야 합니다.

이력 후보가 0명일 때 조건을 자동으로 풀지 않습니다. 플랫폼·예산·규모·카테고리 중 **정확히 하나만 어긋나는** 이력 후보를 점수순으로 최대 3명 보여줍니다. 가장 적게 예산을 올리는 순서는 아니므로 비용 변화가 작은 후보가 먼저 나오지 않을 수 있습니다. 두 조건 이상을 바꿔야 하는 후보까지 확장하지는 않습니다.

## 제외한 기능과 이유

| 제외 범위 | 이유 |
| --- | --- |
| 총예산 배분·여러 명 조합 최적화 | 1명 단위 후보 비교에 집중. 조합별 계약 조건과 실제 단가 데이터가 없음 |
| 실제 견적·섭외·결제·메시지 전송 | 외부 연동과 거래 상태 관리가 필요한 별도 업무 |
| 실제 계정 인증·서버 권한 관리 | 역할별 화면 검증용 정적 프로토타입. 임시 로그인은 보안 장치가 아님 |
| 전환율·매출·ROAS 예측 | 제공 데이터에 구매·전환·캠페인별 성과 레이블이 없음 |
| 신규의 예상 평점·단가로 순위 생성 | 추정값이 확인된 성과로 오인되는 문제를 피하기 위함 |
| 원본 수정·데이터 편집 화면 | 제공 데이터의 재현성을 유지. 원본을 검증한 읽기 전용 DB로 활용 |
| 자동 조건 완화 | 광고주의 제약을 허락 없이 바꾸지 않고 대안을 보고 선택하게 함 |
| 예상 반응 수·반응 1명당 비용 | 팔로워 수와 참여율을 곱한 추정 인원. 점수에 넣지 않기로 한 값을 화면에 두면 실제 반응으로 오해할 수 있어 표에서도 제외 |
| 평점의 표본 수 보정 | 평가 건수가 적은 후보의 평점을 평균 쪽으로 당기는 보정. 받은 평점을 그대로 보이고 건수를 함께 제공하는 편이 판단에 낫다고 보아 제외 |

## 검증과 남은 한계

입력 검증, CSV와 SQLite 전 필드 일치, 평점 공란 27명, 규모 경계, 점수·동점·반올림, 신규 분리, 0명 대안, 정렬·비중·검색 반영 시점은 자동 테스트로 확인합니다. 결과 예시는 README의 체험 조건으로 재현할 수 있습니다.

백분위는 원래 값의 격차를 압축하고, 조회수가 비용 항목에도 들어가며, 플랫폼별 지표 정의 차이와 평점 표본의 불확실성을 완전히 보정하지 못합니다. 출시 판단에는 실제 견적 정확도·후보 검토 완료율·섭외 전환 등 추가 검증이 필요하며, 현재 그런 성과를 측정했다고 주장하지 않습니다.
